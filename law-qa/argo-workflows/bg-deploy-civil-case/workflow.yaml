apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: bg-deploy-civil-case-
spec:
  workflowTemplateRef:
    name: bg-deploy-civil-case
  arguments:
    parameters:
    - name: database-name
      value: default
